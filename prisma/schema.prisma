datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator kysely {
  provider = "prisma-kysely"
  output = "../packages/db/src/types"
  fileName = "index.ts"
}

model User {
  id Int @id @default(autoincrement())

  name String
}
